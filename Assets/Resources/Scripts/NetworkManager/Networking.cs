using UnityEngine;
using System.Collections;

public class Networking : MonoBehaviour {
	
	void OnGUI () {
		if (GUI.Button (new Rect (Screen.width / 2, Screen.height + 50 / 2, 100, 50), "Network")) 
		{
			
		}
		if (GUI.Button (new Rect (Screen.width / 2, Screen.height / 2, 100, 50), "Teste")) 
		{
			
		}
	}
}
